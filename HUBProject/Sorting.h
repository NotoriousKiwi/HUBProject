#pragma once

#include <array>
#include <algorithm>

using std::swap;

class Sorting
{
protected:



private:



public:
	Sorting();

	void Swap(int& a, int& b);
	void BubbleSort(int bubbleArray[], int n);

	~Sorting();
};

